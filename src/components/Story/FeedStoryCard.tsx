// when clicks and opens the story from feed
import React from 'react';

function FeedStoryCard() {
    return (
        <section id="feed_story-card">
    </section>
    );
}

export default FeedStoryCard;